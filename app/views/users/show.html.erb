<section class='user'>
  <h1><%= @user.name %></h1>
  Welcome to my blog!
</section>

<%= if @user.name == 'Krugloff'
      link_to 'New Article', new_article_path, class: 'btn'
    end %>

<%= link_to 'Edit', edit_user_path, class: 'btn' %>
<%= link_to 'Delete', user_path, class: 'btn', method: 'delete' %>